pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        // Add any other plugin repositories here if needed
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
        // Add any other settings repositories here if needed
    }
}
rootProject.name = "PetAdoptionApp"
include ':app'
